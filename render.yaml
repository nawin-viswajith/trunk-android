services:
  - type: web
    name: trunk-backend-dev
    runtime: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    plan: free
    envVars:
      - key: CORS_ORIGINS
        value: "*"
